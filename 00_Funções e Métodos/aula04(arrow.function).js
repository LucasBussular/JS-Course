// função tradicional

const double = function (number) {
    return number*2
}

// arrow function
            // caso só exista um parâmetro para a arrow function, coloca-lo entra parenteses é opicional
const double02 = (number) => {
    return number*2
}

    // caso o bloco de código contenha apenas uma linha retornando um valor, podemos eliminar a palavra "return" e as chaves